services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - NODE_ENV=development
      - PORT=8000
      - JWT_SECRET=dev_secret
    volumes:
      - .:/app
      - /app/node_modules
